import Button from './Button';

const Header= ( props) =>{
    return (
        <header className="header">
            <h5>{ props.title }</h5>
        </header>
    )
}

export default Header; 